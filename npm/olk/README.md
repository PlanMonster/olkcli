# @planmonster/olkcli

A fork of [olkcli](https://github.com/rlrghb/olkcli) with access-token credential injection for ephemeral and sandboxed environments.

Supply a delegated Microsoft Graph token through `OLK_ACCESS_TOKEN`; the CLI will not read or persist stored OAuth credentials.
